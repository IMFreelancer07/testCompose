pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    ext {
        compose_version = "2.7.0-rc01"
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "testCompose"
include ':app'
